import { AllinstructionfilterPipe } from './allinstructionfilter.pipe';

describe('AllinstructionfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AllinstructionfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
